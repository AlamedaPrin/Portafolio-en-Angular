export class Skill {

    id: number;
    tipoDeSkill: string;
    score: number;

    constructor(id: number, tipoDeSkill: string, score: number) 

    {
        this.id = id;
        this.tipoDeSkill = tipoDeSkill;
        this.score = score;
    }
    
}